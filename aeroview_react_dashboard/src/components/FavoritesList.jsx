import React from "react";

/**
 * PUBLIC_INTERFACE
 * List component for user's favorite airports.
 */
const FavoritesList = () => {
  return (
    <nav>
      {/* Favorites List */}
    </nav>
  );
};

export default FavoritesList;
